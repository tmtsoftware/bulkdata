@startuml
participant User
box "Cluster" #LightBlue
	participant Frontend
	participant "Cluster mediator"
end box
User -> Frontend: Connect/Disconnect two servers
Frontend -> "Cluster mediator": Connect/disconnect two servers\nTopic: "subscription"
Frontend -> User: ok
@enduml