include "application.conf"

topology {
  role = "image-filter"

  sources = [
    {
      interface = "localhost"
      port = 8002
    }
  ]

  binding {
    interface = "localhost"
    port = 8003
  }
}
