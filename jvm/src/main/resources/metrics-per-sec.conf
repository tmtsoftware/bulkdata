include "application.conf"

topology {
  role = "metrics-per-sec"

  sources = [
    {
      interface = "localhost"
      port = 8004
    }
  ]

  binding {
    interface = "localhost"
    port = 8006
  }
}
